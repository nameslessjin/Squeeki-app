const INITIAL_STATE = {
  IOSVersion: '1.0.16',
  AndroidVersion: '1.0.16'
};

export default (metadataReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
});
